import React from 'react';
import LoginForm from '../components/auth/LoginForm';

/**
 * Login page component
 * Wrapper for LoginForm
 */
const LoginPage: React.FC = () => {
  return <LoginForm />;
};

export default LoginPage;

